#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <SFML/Graphics.hpp>
bool press1 = false;
bool press2 = false;
extern bool is_but1(sf::Event event);

extern bool is_in_Rect();